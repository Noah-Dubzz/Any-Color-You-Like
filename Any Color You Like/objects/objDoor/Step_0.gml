// Checks if all color collection variables are true
if (global.blue_collected = true && global.green_collected = true && global.red_collected = true) {
    // Changes the color of objDoor to the value of global.DoorColor
    image_blend = global.DoorColor; // Uses image_blend to change the color
	locked = false;
}