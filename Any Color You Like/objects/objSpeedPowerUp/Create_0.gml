// Initialize variables for floating effect
float_amplitude = 5;  // How far it floats up and down
float_speed = 0.25;   // How fast it floats (larger is faster)
initial_y = y;        // Store the original y position
float_offset = irandom(360); // Adds variation for multiple objects
currentshoe_time = 0; // Increments upwards